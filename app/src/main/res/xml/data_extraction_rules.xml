<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <cloud-backup>
        <!-- Exclude sensitive keystore-backed shares or files from standard cloud backups -->
    </cloud-backup>
    <device-transfer>
        <!-- Exclude sensitive SQLite databases from plain transfers if needed -->
    </device-transfer>
</data-extraction-rules>
